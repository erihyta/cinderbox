add_executable(cb_tests test_main.cpp)
target_link_libraries(cb_tests PRIVATE cb_sim cb_anim)
cb_target_warnings(cb_tests)

foreach(name map_format templates gameplay_sanity anim_controller anim_pipeline repeatability snapshot_roundtrip portable_snapshot rollback rollback_reset stress)
	add_test(NAME ${name} COMMAND cb_tests ${name})
endforeach()

add_executable(cb_net_tests net_test.cpp)
target_link_libraries(cb_net_tests PRIVATE cb_server_core cb_client_core)
if(WIN32)
	target_link_libraries(cb_net_tests PRIVATE winmm)
endif()
cb_target_warnings(cb_net_tests)
foreach(name protocol map_sync loopback_session late_join reconnect grace_expiry lossy_session)
	add_test(NAME net_${name} COMMAND cb_net_tests ${name})
endforeach()
